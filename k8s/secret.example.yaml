# NUR DOKUMENTATION - diese Datei wird nie angewendet.
# Das echte Secret erzeugt ./k8s/make-secret.sh zur Laufzeit im Cluster.
apiVersion: v1
kind: Secret
metadata:
  name: zeiterfassung-secret
type: Opaque
stringData:
  SECRET_KEY: "<32 Byte Zufall, z.B. python3 -c 'import secrets;print(secrets.token_hex(32))'>"
  ZE_USERS: "kiril:pbkdf2_sha256$240000$<salt>$<hash>:admin,anna:pbkdf2_sha256$...:user"
