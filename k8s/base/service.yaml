apiVersion: v1
kind: Service
metadata:
  name: zeiterfassung
  labels:
    app: zeiterfassung
spec:
  type: ClusterIP
  selector:
    app: zeiterfassung
  ports:
    - name: http
      port: 80
      targetPort: http
